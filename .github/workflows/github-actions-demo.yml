name: GitHub Actions Demo
run-name: ${{ github.actor }} is testing out GitHub Actions 🚀
on: [push]
jobs:
  Explore-GitHub-Actions:
    runs-on: ubuntu-latest
    steps:
      - run: ls
      - run: mkdir -p ${{ github.workspace }}/tests && echo "Creating test directory" && cp ${{ github.workspace }}/examples/Русский_забор.cpp ${{ github.workspace }}/tests/ && cp ${{ github.workspace }}/examples/связный_список.cpp ${{ github.workspace }}/tests/ && cp ${{ github.workspace }}/examples/Слава_Руси.cpp ${{ github.workspace }}/tests/ && cp ${{ github.workspace }}/examples/Сортировочка_2.cpp ${{ github.workspace }}/tests/ && cp ${{ github.workspace }}/examples/Сортировочка.cpp ${{ github.workspace }}/tests/ && cp ${{ github.workspace }}/header/Ве_крест_крест.h ${{ github.workspace }}/tests/ && mkdir -p ${{ github.workspace }}/tests/cpp && cp ${{ github.workspace }}/header/cpp/sort_static.cpp ${{ github.workspace }}/tests/cpp && echo "Creating test header" && cd ${{ github.workspace }}/tests && echo "Moving test directory" && g++ Русский_забор.cpp && g++ связный_список.cpp && g++ Слава_Руси.cpp && g++ Сортировочка_2.cpp && g++ Сортировочка.cpp && echo "all complete" && cd ${{ github.workspace }} && rm -r ${{ github.workspace }}/tests
